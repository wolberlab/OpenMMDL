name: docs
channels:
  - defaults
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  
  - numpy
  - requests
  - rdkit
  - mdtraj
  - pdbfixer
  - openff-toolkit
  - openmmforcefields
  - cudatoolkit
  - cuda-python
  - MDAnalysis
  - pytest-shutil
  - flask



    # Pip-only installs
  #- pip:

