[tool:pytest]
norecursedirs = .* *.egg* build dist conda.recipe
addopts =
    --junitxml=junit.xml
    --tb native
    --strict-markers
    --durations=20
markers =
    serial: execute test serially (to avoid race conditions)
